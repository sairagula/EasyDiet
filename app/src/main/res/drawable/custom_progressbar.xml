<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/darkGrey"
                android:centerY="1.0"
                android:endColor="@color/darkGrey"
                android:startColor="@color/darkGrey" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/greenAccentColor"
                    android:centerY="1.0"
                    android:endColor="@color/greenAccentColor"
                    android:startColor="@color/greenAccentColor" />
            </shape>
        </clip>
    </item>

</layer-list>